There are a lot of blocks that can benifit from the connected textures feature. But we are humans, and cannot do them all. All the blocks in folders are the ones which support connected textures, while the ones outside do not.

- CoolAbhi1290